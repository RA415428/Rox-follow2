// Copy this file into your project root as: capacitor.config.ts
import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.roxfollow.app',
  appName: 'Rox Follow',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
